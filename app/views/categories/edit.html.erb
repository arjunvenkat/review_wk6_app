<h1>Editing Category #<%= @category.id %></h1>


<%= form_tag(category_url(@category), method: 'put') do %>

  <div>
    <%= label_tag :description %><br />
    <%= text_field_tag :description, @category.description %>
  </div>

  
  <div>
    <%= submit_tag "Update Category" %>
  </div>

<% end %>
